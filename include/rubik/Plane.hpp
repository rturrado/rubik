#ifndef PLANE_HPP
#define PLANE_HPP

namespace Rubik {
namespace Graphics {

enum Plane { x, y, z };

const unsigned int planeBegin = x;
const unsigned int planeEnd   = z;

} // namespace Graphics 
} // namespace Rubik

#endif // ifndef PLANE_HPP
